#include<stdio.h>
int main(){
    char ch,a,e,i,o,u;
    int num;
    scanf("%c",&ch);
    if (ch==a,e,i,o,u);
    printf("Vowel");
    else if (ch!=a,e,i,o,u);
    printf("Consonant");
    else if (ch=int);
    printf("Digit")
    else
    printf("Special Character");
    return 0;
}